---
export interface Props {
    text: string;
    className?: string;
}

const { text, className } = Astro.props;
---

<span class={`text-sm sm:text-base my-6 block font-semibold text-subheading uppercase ${className}`}>
    {text}
</span>

