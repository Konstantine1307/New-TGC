---
export interface Props{
    text?:string;
    className?:string
}

const {text, className} = Astro.props
---

<p class={`sm:text-base midmd:text-lg text-heading-3  ${className}`}>
    {text}

</p>