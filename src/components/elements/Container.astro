---
export interface Props {
  className?: string;
  id?: string;
}

const { className, id } = Astro.props;
---

<div
  class={`mx-auto max-w-7xl w-full px-2 sm:px-6 md:px-14 lg:px-5 ${className}`} ${id}
>
  <slot />
</div>
